function Navbar({ title }) {
  return (
    <div
      style={{
        padding: "16px",
        backgroundColor: "#111827",
        color: "white"
      }}
    >
      <h2>{title}</h2>
    </div>
  )
}

export default Navbar
